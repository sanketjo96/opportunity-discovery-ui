# Local development only. Vercel builds use dashboard env vars + `.env.production` (see `.env.example`).
# Use the Vite dev proxy to this port when `VITE_API_BASE_URL` is empty (see `.env.development`).
VITE_API_BASE_URL=http://127.0.0.1:3000
