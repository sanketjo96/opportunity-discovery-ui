# Production builds (`vite build` / Vercel): set `VITE_API_BASE_URL` in the Vercel project
# (Settings → Environment Variables) to your public API origin, e.g. https://api.example.com
# Leave empty only if the deployed site proxies `/api` to your backend.
VITE_API_BASE_URL=https://opportunity-discovery.onrender.com
