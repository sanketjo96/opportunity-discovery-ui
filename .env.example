# Copy to `.env` for local overrides. On Vercel, add variables in the project Settings → Environment Variables.

# Public API origin, no trailing slash. Required in Production (and Preview) unless `/api` is rewritten on Vercel.
# Example: https://api.yourdomain.com
VITE_API_BASE_URL=
